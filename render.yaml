services:
  - type: web
    name: privacy-chat-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 8765
      - key: PYTHON_VERSION
        value: 3.11
    plan: free
